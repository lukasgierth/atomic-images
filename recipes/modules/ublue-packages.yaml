modules:
  - type: dnf
    repos:
      copr:
        - ublue-os/packages
    cleanup: true
    install:
      packages:
        - ublue-os-libvirt-workarounds
        - ublue-os-udev-rules
        - ublue-polkit-rules
