modules:
  - type: script
    snippets:
      - ls -l ./
      - pwd
      - mount
      - chmod 0755 ./files/bash-scripts/*.sh

  - type: files
    files:
      - source: fish-functions
        destination: /usr/share/fish/vendor_functions.d
      - source: bash-scripts
        destination: /usr/bin
