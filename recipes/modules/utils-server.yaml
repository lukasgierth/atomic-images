modules:
  - type: dnf
    remove:
      packages:
        - "*-audio-firmware"
        - "*-gpu-firmware"
        - nano
        - toolbox
    install:
      packages:
        - curl
        - greenboot-default-health-checks
        - openssh
        - python
        - qemu-guest-agent
        - rsync
